# 개인 맞춤형 실시간 정보판 — 단계별 실행 계획

> 원본 근거 문서: `personal-realtime-dashboard-strategy-complete.md` (사용자 제공, Downloads 폴더)
> 이 프로젝트는 기존 워크스페이스의 다른 폴더(예: `dashboard`, `today-status-board`)를 참고하지 않고 완전히 새로 시작한다.

## 확정 범위

원본 문서는 "위젯을 5개로 제한한다"고만 되어 있고 구체적인 5개 목록은 지정하지 않았다. 문서 내에서 비중과 근거가 가장 뚜렷한 아래 조합을 제안했고, 사용자의 `Codex_작업지시서.md` 구현 요청을 승인으로 반영해 2026-08-26 확정했다.

1. 로스트아크 공지/이벤트
2. 로스트아크 거래장 시세 (재료 1~2종)
3. Upbit 암호화폐 시세 (BTC/KRW)
4. 한국수출입은행 고시 환율
5. GitHub / Discord / Cloudflare 서비스 상태 중 1개

---

## 참고 — 기존 사이트 로아와(loawa.com)와의 관계

사용자가 로스트아크 정보 사이트로 로아와(https://loawa.com)를 제시하여 실제로 확인했다.

**로아와가 이미 제공하는 것**: 캐릭터/다중 검색, 길드, 순위, 통계, 경매 입찰 계산기 등 도구, 시세, 패치 이력, 드레스룸, 미니게임, 명예의 전당, 커뮤니티(전리/제보) 등 로스트아크 정보 포털 전 영역. 홈 화면에는 "등록된 길드/원정대/캐릭터 수" 같은 누적 통계도 있다.

**확인된 사실 — 서버 혼잡도**: 로아와도 실시간 접속자·혼잡도가 아니라 누적 등록 통계(예: 등록된 캐릭터 4,857,679명)만 보여준다. 실시간 서버 혼잡도는 로아와 같은 대형 커뮤니티 사이트도 확보하지 못한 데이터라는 뜻이다.

**결정**: 이 사실은 원본 전략 문서 1.4의 판단(공식 OpenAPI에 서버 혼잡도 API가 없다)을 재확인해줄 뿐, 계획을 바꾸지 않는다. 5단계에서 "게임 서버 점검"과 "OpenAPI 접근 불가"만 구분해 보여주기로 한 결정을 그대로 유지한다 — 혼잡도를 억지로 추정해서 보여주는 기능은 추가하지 않는다.

**범위 판단**: 로아와는 이미 완성된 대형 정보 포털이라 캐릭터 검색·길드 순위·시세·패치 이력까지 동일하게 만드는 것은 이 프로젝트의 목표(개인 맞춤 5개 위젯 실시간 정보판)와 맞지 않는다. 로아와를 따라가지 않고, 원본 문서가 정의한 "위젯마다 원천 시각·조회 시각·출처 URL을 항상 함께 보여준다"는 투명성 원칙에 집중한다 — 이 부분이 로아와에는 없는 차별점이다.

---

## 0단계 — 범위 고정

**작업**
- 위 5개 위젯 확정
- 위젯별 "공급자 / 인증 필요 여부 / 원천 시각 제공 여부 / 권장 갱신 주기" 표 작성

**결정 이유**
원본 문서 5장 로드맵의 첫 단계가 범위 고정이다. 위젯 수를 먼저 못 박아야 이후 API 조사·아키텍처 설계 범위가 흔들리지 않는다. 로스트아크를 포함한 이유는 문서 전체 분량의 절반 이상(1장, 54~161줄)이 로스트아크에 할애되어 있어 이 프로젝트의 핵심 기능으로 판단했기 때문이다.

---

## 1단계 — 프로젝트 초기화

**작업**
- 새 폴더에 `Next.js + TypeScript + Tailwind CSS` 셋업
- TanStack Query 설치

**결정 이유**
문서 4.1/6장이 Next.js를 권장하는 근거는 단 하나, "로스트아크·기상청 같은 키 필요 API의 키를 서버에서 숨겨야 한다"는 것이다(448줄). Vite로 시작하면 나중에 별도 서버나 Worker를 추가해야 하지만, 처음부터 Route Handler를 쓸 수 있는 Next.js로 시작하면 구성이 더 적게 든다. SWR 대신 TanStack Query를 택한 이유는 문서가 코드 예시(329~340줄)를 TanStack Query 기준으로 이미 제공하고 있어 그대로 재사용할 수 있기 때문이다.

---

## 2단계 — 공통 데이터 계약과 정적 UI (fixture 기반)

**작업**
- `WidgetData<T>` 타입 정의
- `WidgetCard` 컴포넌트 구현 (상태 배지, 출처, 원천/조회 시각, 다음 갱신 표시)
- fixture JSON으로 정상/오래됨/점검/실패/제한 5가지 상태 렌더링 검증
- 반응형 그리드 + 다크모드

**결정 이유**
문서 0장이 "모든 위젯 공통 원칙"으로 못 박은 부분이므로 가장 먼저 고정해야 이후 위젯마다 표시 방식이 제각각 벌어지는 것을 막을 수 있다. 실제 API 연동 전에 fixture로 먼저 만드는 이유는 API 키 발급·승인 대기 시간과 무관하게 UI 완성도를 먼저 확보하기 위해서다(문서 5장 단계 1과 동일한 순서).

---

## 3단계 — 무인증 공개 API 연동

**작업**
- `/api/widgets/status` (GitHub/Discord/Cloudflare `summary.json`)
- `/api/widgets/upbit-ticker`
- 브라우저는 이 두 엔드포인트만 호출

**결정 이유**
키가 필요 없는 API부터 붙여서 "서버 경유 → WidgetData 정규화 → 클라이언트 렌더링" 파이프라인 자체를 먼저 검증한다. 키 관리·환경변수 문제와 파이프라인 문제를 분리해서 디버깅하기 위함이다(문서 5장 단계 2, 452줄 질의응답과 동일 근거).

---

## 4단계 — 키 기반 API 연동

**작업**
- Vercel 환경변수에 `LOSTARK_API_KEY`, 수출입은행 `serviceKey` 등 등록 (`NEXT_PUBLIC_` 접두사 절대 금지)
- 공급자별 adapter 모듈 분리
- 공통 timeout/retry/429·503 처리 로직 작성

**결정 이유**
문서 4.4 보안 규칙이 명확하다 — 키는 서버에만 두고, 화면에는 키가 제거된 URL 템플릿만 노출한다. adapter를 공급자별로 분리하는 이유는 로스트아크(429/503, `bearer` 뒤 공백 규칙)와 환율(단순 GET)의 오류 처리 방식이 완전히 달라서, 하나의 함수로 합치면 분기 조건만 늘어나기 때문이다.

---

## 5단계 — 로스트아크 기능 구현

**작업**
- 공지(`/news/notices`) 먼저 구현
- 거래장 시세: `/markets/options` 캐시 → `/markets/items` 검색 순서로 구현
- 점검 상태는 "공지 기반"과 "503 기반"을 별도 배지로 분리 표시

**결정 이유**
`/markets/options`(카테고리 메타데이터, 24시간 캐시)를 먼저 캐시해야 `/markets/items` 검색이 가능한 구조라 순서가 강제된다(96·132줄). "게임 서버 점검"과 "OpenAPI 접근 불가"를 분리 표시하는 이유는 문서 1.4가 명시한 대로 503이 실제 게임 장애를 보장하지 않기 때문이다 — 과장된 안내는 신뢰도를 깎는다.

---

## 6단계 — 운영 품질

**작업**
- 위젯별 서버 캐시 TTL 적용
- 동일 요청 in-flight 공유 (promise dedupe)
- 실패 시 마지막 성공값 유지
- 수동/자동 새로고침 카운트다운 UI

**결정 이유**
문서 4.3의 캐시 원칙과 3.4의 새로고침 UX 규칙을 그대로 반영한다. 특히 "실패해도 마지막 성공 데이터 보존"은 개인 대시보드가 잠깐의 API 장애로 화면이 텅 비는 것을 막는 핵심 요구사항이다(352줄).

---

## 7단계 — 배포 및 체크리스트 검증

**작업**
- Vercel 배포
- Network 탭에서 키 노출 여부 확인
- 문서 7장 체크리스트(보안/데이터 품질/안정성/배포) 항목별로 직접 확인

**결정 이유**
문서가 이미 배포 직전 체크리스트를 표 형태로 제공하므로(480~511줄) 별도로 만들 필요 없이 그대로 검증 스크립트처럼 사용한다.

---

## 8단계 (선택, 나중) — 확장

이력 그래프, 알림, 로그인·기기 간 동기화는 문서 6장 "이력 그래프는 언제 추가해야 하나?" 답변대로 0~7단계가 안정된 뒤에만 착수한다. 지금 단계에서 손대면 DB·보존정책·인증까지 같이 열려서 범위가 무한정 커진다.

---

## 다음 확인 사항

- 로스트아크 OpenAPI 키와 한국수출입은행 키 발급·Vercel 등록
- 실제 배포 URL Network 탭에서 키 비노출 최종 검증
